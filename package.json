{
  "name": "only-tick",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass:dist": "./node_modules/.bin/node-sass --output-style compressed -o css/ sass",
    "sass": "./node_modules/.bin/node-sass -o css/ sass",
    "watch:css": "./node_modules/.bin/watch --wait .1 'npm run sass' sass"
  },
  "author": "Mark Uy",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^3.8.0"
  },
  "description": "",
  "devDependencies": {
    "nodemon": "^1.10.2",
    "watch": "^0.19.2"
  }
}
